package com.ticketing.domain;

public enum Action {
	CREATED, 
	APPROVED, 
	REJECTED, 
	ASSIGNED, 
	RESOLVED, 
	CLOSED, 
	REOPENED,
	UPDATED
//	,
//	PENDING_APPROVAL // Verify if the Controller is correct
}
